<header>
	<div class="row" %>
		<div class="col-md-12">
			<nav class="navbar navbar-default" role="navigation">
			  <div class=".container-fluid">
			    <div class="navbar-header">
			    	<%= link_to "NewSpeak", new_session_path, class: "navbar-brand" %>
			    </div>

			    <div class="collapse navbar-collapse">
				    <ul class="nav navbar-nav navbar-right">
				    	<% if signed_in? %>
				    		<li class="dropdown">
				    		 <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"> <%= current_user.email %> <span class="caret"></span></a>
				    		 <ul class="dropdown-menu" role="menu">
			            <li><%= link_to "Sign out", session_path("current"), method: :delete %></li>
		            </ul>
				    		 </li>
				    	<% else %>
				    		<li> <%= link_to "Sign In", new_session_path, icon: "sign-in"  %> </li>
			    		<% end %>
				    </ul>
			    </div>

			  </div>
			</nav>
		</div>
	</div>
</header>