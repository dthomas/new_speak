<%= content_for(:breadcrumb) do %>
	<ol class="breadcrumb">
	  <li> <%= link_to 'Settings', dashboards_path %> </li>
	  <li> <%= link_to 'Courses', courses_path %> </li>
	  <li> <%= link_to @course.name, course_path(@course) %> </li>
	  <li class="active"><%= @term.title %></li>
	</ol>
<% end %>

<div class="row">
	<div class="col-md-8">
  	<%= simple_form_for(@term, layout: :horizontal) do |f| %>
			<div class="panel panel-default">
			  <div class="panel-heading">
			    <h3 class="panel-title">Editing <%= @term.title %></h3>
			  </div>
			  <div class="panel-body">
			  	<%= render "form", f: f %>
			  </div>
			  <div class="panel-footer">
			  	<%= f.button :submit, 'Add Term' %>
			  </div>
			</div>
	  	<% end %>
	</div>
	<div class="col-md-4">
		<div class="panel panel-default">
			<div class="panel-heading">
				<h3 class="panel-title">Some title</h3>
			</div>

			<div class="panel-body">
				Some help text
			</div>
		</div>
	</div>
</div>
